import "./bootstrap";
import * as bootstrap from "bootstrap";
import "@popperjs/core";
import.meta.glob([
    "../img/**",
    "../fonts/**",
    "../svg/**",
    "../favicon.ico",
    "../robots.txt",
    "../manifest.json",
]);
